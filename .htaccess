# Página de error 404 personalizada
ErrorDocument 404 /404.html

# Activar mod_rewrite

RewriteEngine On

# Fallback: si /post/<slug> no tiene archivo prerenderizado,
# servirlo dinámicamente vía /post.html?slug=<slug>.
# Marca env POST_FALLBACK para bloquear loops con la regla legacy.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME}.html !-f
RewriteRule ^post/([^/.]+)/?$ /post.html?slug=$1 [L,QSA,E=POST_FALLBACK:1]

# Legacy: /post.html?slug=xxx -> /post/xxx (no corre en rewrites internas)
RewriteCond %{ENV:POST_FALLBACK} !=1
RewriteCond %{ENV:REDIRECT_POST_FALLBACK} !=1
RewriteCond %{QUERY_STRING} ^slug=([^&]+)
RewriteRule ^post\.html$ /post/%1? [R=301,L]

# Evitar conflictos con directorios reales

RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f

# Redirigir de .html a la URL sin extensión

RewriteCond %{ENV:POST_FALLBACK} !=1
RewriteCond %{ENV:REDIRECT_POST_FALLBACK} !=1
RewriteCond %{THE_REQUEST} \s/([^.]+)\.html [NC]
RewriteRule ^ /%1 [R=301,L]

# Cargar automáticamente el archivo .html si existe

RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^([^.]+)$ $1.html [L]

# Compresión
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css application/javascript application/json image/svg+xml
</IfModule>

# Cache estático
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 7 days"
    ExpiresByType application/javascript "access plus 7 days"
    ExpiresByType image/webp "access plus 30 days"
    ExpiresByType image/png "access plus 30 days"
    ExpiresByType image/jpeg "access plus 30 days"
    ExpiresByType image/svg+xml "access plus 30 days"
</IfModule>
